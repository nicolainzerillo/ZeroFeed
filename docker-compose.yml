version: '3.8'

services:
  zerofeed-relay:
    build: .
    container_name: zerofeed-relay
    restart: always
    ports:
      - "8443:8443/tcp"
      - "8443:8443/udp"
      - "8444:8444/tcp"
      - "9090:9090/tcp"
    environment:
      - GOMAXPROCS=2
    read_only: true
    tmpfs:
      - /tmp
    deploy:
      resources:
        limits:
          memory: 64M
